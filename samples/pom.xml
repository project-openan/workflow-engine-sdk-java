<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.openan.workflow.sdk</groupId>
        <artifactId>workflow-exec-engine-parent</artifactId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>engine-sdk-samples</artifactId>
    <packaging>jar</packaging>

    <name>engine-sdk-samples</name>
    <description>Sample applications demonstrating the A2A-T workflow execution SDK</description>

    <dependencies>
        <!-- The workflow execution SDK -->
        <dependency>
            <groupId>dev.openan.workflow.sdk</groupId>
            <artifactId>engine-sdk</artifactId>
            <version>${revision}</version>
        </dependency>
        <!-- Spring Boot starter: auto-assembles A2A server beans, partner only implements AgentExecutor -->
        <dependency>
            <groupId>dev.openan.workflow.sdk</groupId>
            <artifactId>engine-sdk-spring-boot-starter</artifactId>
            <version>${revision}</version>
        </dependency>
        <!-- Logging: Log4j2 (SLF4J 2.x binding) with console + rolling file appender -->
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-slf4j2-impl</artifactId>
            <version>${log4j2.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
            <version>${log4j2.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <properties>
        <maven.deploy.skip>true</maven.deploy.skip>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <encoding>${project.build.sourceEncoding}</encoding>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
