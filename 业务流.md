
---

### 表格一：专线业务投诉诊断接口（核心诊断）

| 项目 | 详细说明 |
| :--- | :--- |
| **接口名称** | 专线业务投诉诊断接口 |
| **协议/模式** | A2A-T Task-T |
| **调用方向** | 工作台智能体 -> SPN Domain Agent（OMC） |
| **功能描述** | 工作台智能体下发诊断请求，OMC完成网络侧故障根因诊断并返回结果。 |
| **请求参数** | 1. 投诉发生时间<br>2. 业务投诉故障现象<br>3. 专线分段业务标识<br>4. 故障场景（专线业务质差 / 专线中断） |
| **响应内容** | **一期必须返回：** <br>1. 故障根因<br>2. 根因点位置<br>3. 根因描述<br>4. 修复建议<br><br>**二期新增返回：** <br>1. OMC是否支持业务自动抢通<br>2. 业务抢通的方案详情 |


### 表格二：专线业务投诉诊断协商接口（参数修正）

| 项目 | 详细说明 |
| :--- | :--- |
| **接口名称** | 专线业务投诉诊断协商接口 |
| **协议/模式** | A2A-T Negotiation-T |
| **调用方向** | SPN Domain Agent（OMC） <--> 工作台智能体（双向） |
| **功能描述** | 当OMC发现任务参数有误或缺失时，反向发起协商，请求工作台智能体补全或修正信息。 |
| **协商请求（OMC→工作台）** | **触发条件：** <br>1. 必选参数缺失（如故障发生时间）<br>2. 参数取值错误（如故障现象不在枚举范围内）<br>3. 参数语义校验错误（如应为逻辑口却下发了物理口）<br><br>**请求内容：** 明确指出缺失/错误的参数及修正建议。 |
| **协商响应（工作台→OMC）** | 工作台智能体理解协商请求，补全或修正相关参数，并返回给OMC。 |
| **协商完成（OMC）** | OMC确认协商信息完成后，继续执行诊断任务，并最终返回诊断结果（同表格一响应内容）。 |


### 表格三：业务抢通消息订阅与上报接口（结果通知）

| 项目 | 详细说明 |
| :--- | :--- |
| **接口名称** | 业务抢通消息订阅接口 / 消息上报 |
| **协议/模式** | A2A-T Notification-T |
| **功能描述** | 用于工作台订阅抢通结果，以及OMC通过SSE流主动上报抢通结果消息。 |
| **订阅请求（工作台→OMC）** | **包含内容：** <br>1. 消息主题<br>2. 上报消息数据格式（需包括：投诉事件流水号、分段业务标识、抢通方案执行结果、抢通方案详情） |
| **订阅响应（OMC→工作台）** | **成功：** 返回订阅成功。<br>**失败：** 返回订阅失败及失败原因描述。 |
| **消息上报（OMC→工作台）** | 基于订阅时指定的数据格式，通过**SSE流**主动向工作台推送业务抢通结果消息。 |


### 表格四：专线业务抢通授权接口（自动执行授权）

| 项目 | 详细说明 |
| :--- | :--- |
| **接口名称** | 专线业务抢通授权接口 |
| **协议/模式** | A2A-T Authorization-T |
| **功能描述** | 工作台向OMC下发或取消抢通授权，OMC据此决定是否自动执行抢通。 |
| **授权请求（工作台→OMC）** | **包含内容：** <br>1. 操作类型：新增授权 / 取消授权<br>2. 操作名称：隧道调优（或其他业务抢通操作） |
| **授权响应（OMC→工作台）** | **成功：** 返回授权成功。<br>**失败：** 返回授权失败及详细的失败原因。 |
| **后置操作（OMC内部）** | 授权成功后，当OMC在执行诊断后的业务抢通时，若匹配到已授权项，则**自动执行**业务抢通，并依据**表格三**的订阅通道上报抢通结果消息。 |
